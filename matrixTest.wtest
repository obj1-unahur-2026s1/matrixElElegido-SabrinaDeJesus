import pasajeros.*
import nave.*

describe "Matrix " {

  test "Manejo de error en test" {
    //Escribo el error que quiero que muestre en consola
    asset.throwsExceptionWithMessage("La lista esta vacia", {nave.pasajeroAlAzar()})
  }

}